```
┌─────────────────────────────────────────────────────────────────────┐
│                    AUDIO INTEGRATION ARCHITECTURE                    │
└─────────────────────────────────────────────────────────────────────┘

USER FLOW:
──────────

   [Browser UI]                [Server]                [OpenAI API]
       │                           │                         │
       │  1. Create AI Billboard   │                         │
       ├──────────────────────────►│                         │
       │   POST /run/billboard     │                         │
       │   { action: "generateAI...│                         │
       │                           │                         │
       │                           │  2. Generate Text       │
       │                           ├────────────────────────►│
       │                           │  GPT-3.5-turbo          │
       │                           │◄────────────────────────┤
       │                           │  "SUMMER SALE..."       │
       │                           │                         │
       │                           │  3. Generate Image      │
       │                           ├────────────────────────►│
       │                           │  DALL-E                 │
       │                           │◄────────────────────────┤
       │                           │  base64 image           │
       │                           │                         │
       │                           │  4. Generate Audio ✨   │
       │                           ├────────────────────────►│
       │                           │  TTS "SUMMER SALE..."   │
       │                           │◄────────────────────────┤
       │  5. Billboard + Audio ID  │  MP3 buffer             │
       │◄──────────────────────────┤                         │
       │  { text, image, audioId } │  Store in memory        │
       │                           │  audioFiles.set(id, buf)│
       │                           │                         │
       │  [Billboard renders       │                         │
       │   with 🔊 PLAY button]    │                         │
       │                           │                         │
       │  6. User clicks PLAY      │                         │
       ├──────────────────────────►│                         │
       │  GET /audio/billboard_123 │                         │
       │                           │                         │
       │  7. Audio stream          │                         │
       │◄──────────────────────────┤                         │
       │  audio/mpeg MP3 data      │  audioFiles.get(id)     │
       │                           │                         │
       │  [HTML5 Audio plays]      │                         │
       │  Button: 🔊 → ⏸️         │                         │
       │                           │                         │

COMPONENT DETAILS:
──────────────────

┌──────────────────────────────────────────────────────────────┐
│ SERVER-SIDE: OpenAI Plugin                                   │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  Class: OpenAIPlugin                                         │
│  File: server/plugins/openai.plugin.js                       │
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │ async generateAudio({ text, voice, instructions }) │     │
│  │                                                    │     │
│  │  1. Call OpenAI TTS API                           │     │
│  │     model: "gpt-4o-mini-tts"                      │     │
│  │     voice: "coral" (default)                      │     │
│  │     input: billboard text                         │     │
│  │                                                    │     │
│  │  2. Convert response to Buffer                    │     │
│  │     const buffer = Buffer.from(mp3.arrayBuffer()) │     │
│  │                                                    │     │
│  │  3. Store in memory                               │     │
│  │     this.audioFiles.set(audioId, buffer)          │     │
│  │                                                    │     │
│  │  4. Return metadata                               │     │
│  │     { success: true, audioId, size, format }      │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │ getAudioFile(audioId)                             │     │
│  │  → Returns Buffer for streaming to browser        │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ SERVER-SIDE: Billboard Plugin                                │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  Class: BillboardPlugin                                      │
│  File: server/plugins/billboard.plugin.js                    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │ async generateAIBillboard({ description, kernel })│     │
│  │                                                    │     │
│  │  1. Generate text with LLM                        │     │
│  │  2. Select template & colors                      │     │
│  │  3. Generate image (if needed)                    │     │
│  │  4. ✨ Generate audio                             │     │
│  │     await kernel.run('llm', {                     │     │
│  │       action: 'generateAudio',                    │     │
│  │       text: billboardText,                        │     │
│  │       voice: 'coral'                              │     │
│  │     })                                            │     │
│  │  5. Return complete config with audioId           │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │ createBillboard({ text, audioId, ... })           │     │
│  │  → Stores audioId with billboard data             │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ SERVER-SIDE: API Endpoints                                   │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  File: server/index.js                                       │
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │ GET /audio/:audioId                               │     │
│  │                                                    │     │
│  │  1. Get LLM plugin                                │     │
│  │     const llmPlugin = kernel.plugins.llm          │     │
│  │                                                    │     │
│  │  2. Retrieve audio buffer                         │     │
│  │     const buffer = llmPlugin.getAudioFile(id)     │     │
│  │                                                    │     │
│  │  3. Set headers                                   │     │
│  │     Content-Type: audio/mpeg                      │     │
│  │     Content-Length: buffer.length                 │     │
│  │                                                    │     │
│  │  4. Stream to browser                             │     │
│  │     res.send(buffer)                              │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ CLIENT-SIDE: UI & Playback                                   │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  File: public/index.html                                     │
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │ renderFeed()                                       │     │
│  │                                                    │     │
│  │  Renders billboard card with:                     │     │
│  │  - Text content                                   │     │
│  │  - Background/text colors                         │     │
│  │  - Image (if exists)                              │     │
│  │  - ✨ Audio play button (if audioId exists)      │     │
│  │                                                    │     │
│  │  ${billboard.audioId ? `                          │     │
│  │    <button onclick="playBillboardAudio(...)">     │     │
│  │      🔊 PLAY AUDIO                                │     │
│  │    </button>                                      │     │
│  │  ` : ''}                                          │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │ playBillboardAudio(audioId, billboardId)          │     │
│  │                                                    │     │
│  │  1. Stop current audio (if playing)               │     │
│  │     if (currentAudio) currentAudio.pause()        │     │
│  │                                                    │     │
│  │  2. Create new Audio element                      │     │
│  │     const audio = new Audio(`/audio/${audioId}`)  │     │
│  │                                                    │     │
│  │  3. Update button UI                              │     │
│  │     button: 🔊 PLAY → ⏸️ PLAYING...              │     │
│  │                                                    │     │
│  │  4. Play audio                                    │     │
│  │     audio.play()                                  │     │
│  │                                                    │     │
│  │  5. Handle events                                 │     │
│  │     onended: reset button                         │     │
│  │     onerror: show error, reset                    │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
└──────────────────────────────────────────────────────────────┘

DATA FLOW:
──────────

Billboard Object Structure:
{
  id: 123,
  text: "SUMMER SALE\n50% OFF\nLIMITED TIME",
  bgColor: "#FF0000",
  textColor: "#FFFFFF",
  imageData: "data:image/png;base64...",
  audioId: "billboard_1737585600000",  ✨ NEW!
  author: "AI Assistant",
  timestamp: "2026-01-22T...",
  likes: 0,
  aiGenerated: true
}

Audio Storage (Server):
Map {
  "billboard_1737585600000" => Buffer<MP3 data>,
  "billboard_1737585601000" => Buffer<MP3 data>,
  ...
}

FEATURE HIGHLIGHTS:
───────────────────

✨ Automatic Generation
   - AI billboards automatically get audio
   - No extra user action needed

🎵 Browser Playback  
   - HTML5 Audio API
   - No plugins required
   - Works on all devices

🎨 Visual Feedback
   - 🔊 PLAY AUDIO (ready)
   - ⏸️ PLAYING... (active)
   - ❌ ERROR (failed)

🚫 Single Playback
   - Only one audio at a time
   - Auto-stops previous audio

🛡️ Error Handling
   - Billboard creation continues if audio fails
   - Playback errors show user-friendly messages
   - Graceful degradation

VOICE OPTIONS:
──────────────

Available TTS voices:
• coral   - Cheerful, friendly female (DEFAULT)
• alloy   - Neutral, balanced
• echo    - Male, clear
• fable   - Expressive storytelling
• onyx    - Deep, authoritative male
• nova    - Energetic, engaging female
• shimmer - Warm, friendly female
```
